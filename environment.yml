name: nlpmaps
channels:
  - defaults
dependencies:
name: my_env
channels:
  - defaults
dependencies:
  - pip
  - pip:
    - tensorflow-text==2.11.*
    - tf-models-official==2.11.0
    - gensim
    - numpy
    - pandas
    - scikit-learn
    - matplotlib
    - seaborn
    - xgboost
    - nltk
    - spacy
    - keras
    - tensorflow_hub
    - tensorflow
    - datasets
    - requests
    - unittest

prefix: /Users/Jlonging/miniconda3/envs/nlpmaps
